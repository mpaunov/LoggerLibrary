import utils.enums.ReportLevel;
import utils.helpers.FileHelper;
import utils.loggers.Logger;
import utils.loggers.MessageLogger;
import utils.loggers.layouts.*;
import utils.loggers.appenders.*;
import utils.runnables.Starter;

import java.util.Scanner;

// made a simple change so we have our main class nice and clear :D
public class Main {
    public static void main(String[] args) {
        Starter.main(null);
    }
}